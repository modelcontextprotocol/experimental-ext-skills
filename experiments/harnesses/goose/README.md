# skills-e2e-goose

Scenario #1 harness for `olaservo/goose @ mcp-skills-sep`. Python
subprocess driving `goose run --output-format stream-json`; the `goose`
binary comes from `cargo install --git … --branch mcp-skills-sep …`.

- **Runbook + env-var reference + pass-criteria details**: the
  docstring at the top of `agent.py` (full cargo-install command lives
  there).
- **Cross-client orientation** (MCP server launch, scaffolding, the
  other three harnesses): the workspace `experiments/CLAUDE.md`.
