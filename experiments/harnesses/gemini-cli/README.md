# skills-e2e-gemini-cli

Scenario #1 harness for `olaservo/gemini-cli @ experimental/skills-over-mcp`.
Python subprocess driving `node $GEMINI_CLI_ROOT/bundle/gemini.js`.

- **Runbook + env-var reference + pass-criteria details**: the
  docstring at the top of `agent.py` (explains how to clone the fork
  branch, `npm run build` the bundle, and set `GEMINI_CLI_ROOT`).
- **Cross-client orientation** (MCP server launch, scaffolding, the
  other three harnesses): the workspace `experiments/CLAUDE.md`.
